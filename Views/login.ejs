<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Custom CSS to center align the form elements */
    body {
      background: linear-gradient(to right, #73fff6, #00b7ff);
    }

    .center-align {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 120px;
    }

    .form-field {
      width: 300px;
      position: relative;
      /* Position for icons */

    }

    .form-control {
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }

    .form-box {
      max-width: 500px;
      /* Increased width */
      width: 100%;
      padding: 20px;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .btn {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 300px;
    }

    .line-between {
      width: 100%;
      height: 1px;
      background-color: #bab9b9;
      margin: 10px 0;
    }

    .not-registered-button {
      background-color: transparent;
      border: none;
      color: #000;
      font-weight: bold;
      cursor: pointer;
      text-decoration: underline;
      padding: 0;
      margin: 0;
    }

    /* Additional CSS for Navbar */
    .navbar {
      background-color: #185899;
      padding: 10px;
    }

    .navbar .nav-item {
      margin-right: 10px;
    }

    .navbar .nav-item a {
      text-decoration: none;
      color: #fffcfc;
      font-weight: bold;
      transition: color 0.3s;
    }

    .navbar .nav-item a:hover {
      color: #12c59e;
    }

    .navbar .nav-item a.active {
      color: #000000 !important;
    }
  </style>
</head>

<body>
  <nav class="navbar">
    <ul class="nav">
      <li class="nav-item">
        <a class="nav-link <%- activePage === 'index' ? 'active' : '' %>" href="/">
          <i class="fas fa-home"></i> Home
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%- activePage === 'createuser' ? 'active' : '' %>" href="/createuser">
          <i class="fas fa-user-plus"></i> Register
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%- activePage === 'login' ? 'active' : '' %>" href="/login">
          <i class="fas fa-sign-in-alt"></i> Login
        </a>
      </li>
    </ul>
  </nav>
  <form action="/login" method="POST" class="center-align">
    <div class="form-box">
      <%- include('extras/flash') %>

      <h1 class="text-center">Login User</h1><br>
      <div class="form-field mb-3">
        <i class="fas fa-envelope"></i>
        <label for="email" class="form-label">Email:</label>
        <input type="email" class="form-control" name="email" required>
      </div>

      <div class="form-field mb-3">
        <i class="fas fa-lock"></i>
        <label for="password" class="form-label">Password:</label>
        <input type="password" class="form-control" name="password" required>

        <button type="submit" class="btn btn-primary mt-3">
          <span class="mr-2">Login <i class="fas fa-sign-in-alt"></i></span>
        </button>
      </div>

      <div class="mb-3">
        <a href="/forgotPassword">Forgot Password?</a>
      </div>

      <div class="line-between"></div>

      <button type="button" class="btn btn-link not-registered-button" onclick="location.href='/createuser'">Not
        Registered?</button>

    </div>
  </form>

  <!-- Add Font Awesome JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
</body>

</html>
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Email Form</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Custom CSS to center align the form elements */
    body {
      background: linear-gradient(to right, #73c8ff, #0086ff);
    }
    
    .center-align {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 50px;
    }
    
    .form-field {
      width: 300px;
      position: relative; /* Position for icons */
    }
    
    .form-control {
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }
    
    .form-box {
      max-width: 500px; /* Increased width */
      width: 100%;
      padding: 20px;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      display: flex; 
      flex-direction: column; 
      align-items: center;
      justify-content: center; 
    }

    .btn {
      display: flex;
      align-items: center;
      justify-content: center;
    }

  </style>
</head>

<body>
  <form action="/sendEmail" method="post" class="center-align">
    <div class="form-box">
      <h1 class="text-center">Email Form</h1><br>
      <div class="form-field mb-3">
        <i class="fas fa-envelope"></i>
        <label for="subject" class="form-label">Subject:</label>
        <input type="text" class="form-control" name="subject" required>
      </div>

      <div class="form-field mb-3">
        <i class="fas fa-pencil-alt"></i>
        <label for="message" class="form-label">Message:</label>
        <textarea name="message" rows="4" cols="50" class="form-control" required></textarea>
      </div>

      <button type="submit" class="btn btn-primary mt-3">
        <span class="mr-2">Send <i class="fas fa-paper-plane"></i></span> 
      </button>
    </div>
  </form>
</body>

</html>

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile</title>
  <!-- Add Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <!-- Add Font Awesome CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    /* Custom CSS to center align the form elements */
    body {
      background: linear-gradient(to right, #73fff6, #00b7ff);
    }

    .center-align {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-top: 15px;
    }

    .form-field {
      width: 300px;
      position: relative;
      /* Position for icons */
    }

    .form-control {
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }

    .form-box {
      margin-top: 35px;
      max-width: 500px;
      height: 600px;
      /* Increased width */
      width: 100%;
      padding: 20px;
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .btn {
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: rgb(18, 166, 18);
      color: #fff;
      border: #333;
      width: 300px;
    }

    .line-between {
      width: 100%;
      height: 1px;
      background-color: #bab9b9;
      margin: 10px 0;
    }

    .registered-button {
      background-color: transparent;
      border: none;
      color: #000;
      font-weight: bold;
      cursor: pointer;
      text-decoration: underline;
      padding: 0;
      margin: 0;
    }
  </style>
</head>

<body>
 
  <div class="center-align">
    <div class="form-box">
      <h1 class="text-center">User Profile</h1>

      <div class="form-field mb-3">
        <img src="<%= user.image %>" alt="User Image" style="width: 100px; height: 100px; border-radius: 50%; margin-left: 30%;">
      </div>

      <div class="form-field mb-3">
        <i class="fas fa-user"></i>
        <label for="name" class="form-label">Name:</label>
        <input type="text" class="form-control" name="name" value="<%- user.name %>" readonly>
      </div>

      <div class="form-field mb-3">
        <i class="fas fa-map-marker-alt"></i>
        <label for="address" class="form-label">Address:</label>
        <input type="address" class="form-control" name="address" value="<%- user.address %>" readonly>
      </div>

      <div class="form-field mb-3">
        <i class="fas fa-envelope"></i>
        <label for="email" class="form-label">Email:</label>
        <input type="email" class="form-control" name="email" value="<%- user.email %>" readonly>
      </div>

      <div class="line-between"></div>

      <button type="button" class="btn btn-primary mt-3" onclick="location.href='/editprofile/<%- user.id%>'">
        <i class="fas fa-user-edit"></i> Edit Profile
      </button>

      <button type="button" class="btn btn-danger mt-3" onclick="location.href='/deleteprofile/<%- user.id%>'">
        <i class="fas fa-trash-alt"></i> Delete Account
      </button>
    </div>
  </div>

  <!-- Add Font Awesome JS -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
</body>

</html>
